# Migration `20190729224547-init`

This migration has been generated by Marc Stammerjohann at 7/29/2019, 10:45:47 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "dev"."User"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"email" TEXT NOT NULL DEFAULT '' ,"password" TEXT NOT NULL DEFAULT '' ,"firstname" TEXT   ,"lastname" TEXT   ,"role" TEXT NOT NULL DEFAULT 'ADMIN' ,PRIMARY KEY ("id"));

CREATE TABLE "dev"."Post"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"published" BOOLEAN NOT NULL DEFAULT false ,"title" TEXT NOT NULL DEFAULT '' ,"content" TEXT   ,"author" TEXT   REFERENCES User(id),PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "dev"."User.id._UNIQUE" ON "User"("id")

CREATE UNIQUE INDEX "dev"."User.email._UNIQUE" ON "User"("email")

CREATE UNIQUE INDEX "dev"."Post.id._UNIQUE" ON "Post"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190729224547-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,35 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:../db/dev.db"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model User {
+  id        String   @default(cuid()) @id @unique
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  email     String   @unique
+  password  String
+  firstname String?
+  lastname  String?
+  posts     Post[]
+  role      Role
+}
+
+model Post {
+  id        String   @default(cuid()) @id @unique
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  published Boolean
+  title     String
+  content   String?
+  author    User?
+}
+
+enum Role {
+  ADMIN
+  USER
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190729224547-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190729224547-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
