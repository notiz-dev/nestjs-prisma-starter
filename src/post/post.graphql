scalar DateTime

type Query {
  publishedPosts: [Post!]!
  post(postId: ID!): Post
  userPosts(userId: ID!): [Post!]!
}

type Post {
  id: ID!
  createdAt: DateTime!
  title: String!
  author: User!
}
